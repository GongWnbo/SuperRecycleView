<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.gwb.superrecycleview">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme">
        <activity
            android:name=".ui.activity.HeaderAndFooterActivity"
            android:configChanges="orientation|locale|screenSize"/>
        <activity
            android:name=".ui.activity.SteamActivity"
            android:configChanges="orientation|locale|screenSize"/>
        <activity
            android:name=".ui.activity.SkuActivity"
            android:configChanges="orientation|locale|screenSize"
            android:theme="@style/TranslucentTheme"/>
        <activity
            android:name=".ui.activity.ShoppingGoodsActivity"
            android:configChanges="orientation|locale|screenSize">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN"/>-->

            <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>